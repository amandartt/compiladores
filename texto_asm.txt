a: short 2;
i: long 0;

long add() return 2;

long sub(long m, long n) {print m "\n"; print n "\n"; return 1;};

long main()
{	
 	/*a = sub(a, 10);
	print a "\n";

	a = add();
	print a "\n";

	a = a * 2;

	print a "\n";

	a = a/2;

	print a "\n";
 	a = 0;
	while(a<2 || a <5){
		print "dentro while\n";
		a = a + 1;
	};
	print "fora while";
	print "a:" a "\n";
	a = 250;
	when (a>11) then print "ok1 a > 11" else print "ok2 a <= 11";*/
	when (!(a == a)) then else;
 return a;
};
